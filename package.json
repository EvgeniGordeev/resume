{
  "name": "evgeni-gordeev-resume",
  "version": "0.0.1",
  "description": "Description for qcsv",
  "private": true,
  "cacheDirectories": [
    "node_modules"
  ],
  "devDependencies": {
    "http-server": "*"
  },
  "engines": {
    "node": "^7.0"
  },
  "scripts": {
    "prestart": "npm i",
    "start": "node ./node_modules/.bin/http-server ./ -o -p 3001",
    "deploy": "./deploy.sh"
  }
}
